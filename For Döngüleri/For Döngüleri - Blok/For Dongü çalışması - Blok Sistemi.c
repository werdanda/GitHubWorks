#include <stdio.h>
#include <conio.h>

int main()
{
    int satir,sutun,blok;
    for(satir=1; satir<=3; satir++)
        {
            for(sutun=1; sutun<=4; sutun++)
                {
                    for(blok=1; blok<=satir; blok++)
                        {
                            printf("+");
                        }
                    printf("\n");
                }
        }










}
